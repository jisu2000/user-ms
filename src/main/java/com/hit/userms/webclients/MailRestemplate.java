package com.hit.userms.webclients;import com.hit.userms.request.EmailRequest;import com.hit.userms.response.MailResponseBack;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.beans.factory.annotation.Value;import org.springframework.http.HttpStatus;import org.springframework.http.ResponseEntity;import org.springframework.stereotype.Service;import org.springframework.web.client.HttpClientErrorException;import org.springframework.web.client.RestTemplate;@Servicepublic class MailRestemplate {    @Autowired    private RestTemplate restTemplate;    @Value("${email.service}")    private String EMAIL_URL;    public boolean sendMail(EmailRequest request) {        try {            ResponseEntity<MailResponseBack> responseEntity = this.restTemplate.postForEntity(EMAIL_URL + "/send-mail", request, MailResponseBack.class);            if (responseEntity.getStatusCode().is2xxSuccessful()) {                MailResponseBack responseBody = responseEntity.getBody();                System.out.println("Email sent successfully. Response: " + responseBody);                return true;            } else {                System.out.println("Failed to send email. Response status: " + responseEntity.getStatusCodeValue());                return false;            }        } catch (HttpClientErrorException e) {            // Handle HTTP client errors (4xx status codes)            System.out.println("HTTP client error: " + e.getMessage());            return false;        } catch (Exception e) {            // Handle other exceptions            System.out.println("Error sending email: " + e.getMessage());            return false;        }    }}